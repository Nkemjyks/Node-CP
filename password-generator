//...import generate-password. 
let gen = require('generate-password');

// function to generates random password
let pwd = gen.generate({
	length: 8,
	numbers: true
});

// log password
console.log(pwd);
